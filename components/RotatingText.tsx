"use client";import{AnimatePresence,motion}from"framer-motion";import{useEffect,useState}from"react";interface RotatingTextProps{messages:string[];interval?:number;className?:string;}export default function RotatingText({messages,interval=5500,className=""}:RotatingTextProps){const[idx,setIdx]=useState(0);useEffect(()=>{const t=setInterval(()=>{setIdx((i)=>(i+1)%messages.length);},interval);return()=>clearInterval(t);},[messages.length,interval]);return(<div className={`relative h-6 ${className}`}><AnimatePresence mode="wait"><motion.span key={idx} initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} transition={{duration:1.1,ease:[0.19,1,0.22,1]}} className="absolute inset-0 flex items-center justify-center text-[13px] tracking-wide text-ink-dim font-light">{messages[idx]}</motion.span></AnimatePresence></div>);}