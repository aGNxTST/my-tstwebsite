"use client";import{useEffect,useState}from"react";import{motion,AnimatePresence}from"framer-motion";import{ROTATING_MESSAGES}from"@/lib/constants";const EASE=[0.19,1,0.22,1]as const;function playBass(){try{const AudioCtx=window.AudioContext||(window as unknown as{webkitAudioContext:typeof AudioContext}).webkitAudioContext;const ctx=new AudioCtx();const osc=ctx.createOscillator();const gain=ctx.createGain();osc.type="sine";osc.frequency.setValueAtTime(70,ctx.currentTime);osc.frequency.exponentialRampToValueAtTime(38,ctx.currentTime+0.6);gain.gain.setValueAtTime(0.0001,ctx.currentTime);gain.gain.exponentialRampToValueAtTime(0.22,ctx.currentTime+0.05);gain.gain.exponentialRampToValueAtTime(0.0001,ctx.currentTime+0.9);osc.connect(gain).connect(ctx.destination);osc.start();osc.stop(ctx.currentTime+1);}catch{}}export default function Loader({onComplete}:{onComplete:()=>void}){const[msgIdx,setMsgIdx]=useState(0);const[visible,setVisible]=useState(true);useEffect(()=>{const msgTimer=setInterval(()=>{setMsgIdx((i)=>(i+1)%ROTATING_MESSAGES.length);},1600);const doneTimer=setTimeout(()=>{playBass();setVisible(false);setTimeout(onComplete,1000);},3600);return()=>{clearInterval(msgTimer);clearTimeout(doneTimer);};},[onComplete]);return(<AnimatePresence>{visible&&(<motion.div exit={{opacity:0}} transition={{duration:1,ease:EASE}} className="fixed inset-0 z-[100] bg-black flex flex-col items-center justify-center"><motion.div initial={{width:0}} animate={{width:220}} transition={{duration:1.6,ease:EASE}} className="h-px bg-gradient-to-r from-transparent via-gold-bright to-transparent mb-8"/><motion.div initial={{opacity:0,y:10}} animate={{opacity:1,y:0}} transition={{duration:1,delay:1.4}} className="font-display text-[15px] tracking-[0.5em] text-gold uppercase mb-2.5">TST</motion.div><motion.div initial={{opacity:0,y:10}} animate={{opacity:1,y:0}} transition={{duration:1,delay:1.7}} className="font-display font-normal text-[clamp(28px,5vw,44px)] tracking-[0.18em] uppercase">The Street Talks</motion.div><motion.div initial={{opacity:0,y:10}} animate={{opacity:1,y:0}} transition={{duration:1,delay:2.2}} className="mt-5 h-5 text-xs tracking-wide text-ink-dim">{ROTATING_MESSAGES[msgIdx]}</motion.div></motion.div>)}</AnimatePresence>);}